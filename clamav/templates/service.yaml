apiVersion: v1
kind: Service
metadata:
  name: {{ include "clamav.fullname" . }}
  labels:
    {{- include "clamav.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: clamd
      protocol: TCP
      name: clamd
  {{- if .Values.milter.enabled }}
    - port: {{ .Values.service.milterPort }}
      targetPort: milter
      protocol: TCP
      name: milter
  {{- end }}
  selector:
    {{- include "clamav.selectorLabels" . | nindent 4 }}
